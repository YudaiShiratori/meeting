<h3><%= @user.name %>面接官のページ</h3>

<div class="icon">
  <%= image_tag @user.image %>
</div>

<h3><%= @user.name %></h3>
<h4>
  <%= @user.introduction %>
</h4>

<% if logged_in? && current_user.try(:user_id) == current_user.id %>
  <h5>*このページが応募ユーザーから見えるページです</h5>
<% end %>
<%= button_to 'マイページを編集する', edit_user_path(current_user.id), class: "btn btn-info" %>